"use client";

export default function Providers({ children }: Props) {

    return (
        <>
            {/* <NextUIProvider>{children}</NextUIProvider> */}
            {children}
        </>
    );
}